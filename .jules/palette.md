## 2024-05-30 - Semantic form labels
**Learning:** Upgrading non-semantic tags like `<p>` and `<span>` to `<label for="...">` and adding visually hidden `.sr-only` labels to inputs without visible labels improves screen reader and keyboard accessibility significantly.
**Action:** Ensure that all inputs have an associated explicit `<label>` element.
